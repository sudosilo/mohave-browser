plugins {
    id "com.android.application"
}

android {
    namespace "com.mohave.browser"
    compileSdk 36

    defaultConfig {
        applicationId "com.mohave.browser"
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "0.1"
        ndk {
            abiFilters "arm64-v8a"
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation "org.mozilla.geckoview:geckoview:+"
}
